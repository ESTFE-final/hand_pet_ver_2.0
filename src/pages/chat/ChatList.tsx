import React from "react";

interface ChatListProps {
    //
}

const ChatList = ({} : ChatListProps) => {
    return(
        <>ChatList</>
        // <Link></Link> -> HTML a태그
    );
}

export default ChatList;