import React from "react";

interface MainProps {
    //
};

const Main = ({}: MainProps) => {
    return(
        <div style={{height: '2000px', backgroundColor: 'lightgray', color: 'white'}}>Main</div>
    );
};

export default Main;